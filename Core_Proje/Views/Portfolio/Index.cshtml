@using EntityLayer.Concrete
@model List<Portfolio>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
<div class="card-header border-0 d-flex justify-content-between align-items-center">
    <h3 class="mb-2">Proje Listesi</h3>
    <a href="/Portfolio/AddPortfolio" class="btn btn-info">Proje Ekle</a>
</div>
<!-- Light table -->
<div class="table-responsive">
    <table class="table align-items-center table-flush">
        <thead class="thead-light">
            <tr>
                <th scope="col" class="sort" data-sort="name">#</th>
                <th scope="col" class="sort" data-sort="budget">Başlık</th>
                <th scope="col" class="sort" data-sort="budget">ImageURL</th>
                <th scope="col" class="sort" data-sort="budget">ImageURL2</th>
                <th scope="col" class="sort" data-sort="budget">ProjectURL</th>
            </tr>
        </thead>
        <tbody class="list">
            @foreach (var item in Model)
            {
                <tr>
                    <th scope="row">
                        <div class="media align-items-center">
                            <div class="media-body">
                                <span class="name mb-0 text-sm">@item.PortfolioID</span>
                            </div>
                        </div>
                    </th>
                    <td class="budget">
                        @item.Name
                    </td>
                    <td class="budget">
                        @item.ImageURL
                    </td>
                    <td class="budget">
                        @item.ImageURL2
                    </td>
                    <td class="budget">
                        @item.ProjectURL
                    </td>
                    <td>
                        <a href="/Portfolio/EditPortfolio/@item.PortfolioID" class="btn btn-warning">Düzenle</a>
                    </td>
                    <td>
                        <a href="/Portfolio/DeletePortfolio/@item.PortfolioID" class="btn btn-danger">Sil</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>